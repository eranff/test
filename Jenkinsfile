pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo build-1'
                sh 'echo build-2'
            }
        }
        stage('Test') {
            steps {
                sh 'echo test-1'
            }
        }
    }
}